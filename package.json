{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run build:articles && npm run build:projects && nuxt build",
    "start": "node .output/server/index.mjs",
    "dev": "npm run build:articles && npm run build:projects && nuxt dev",
    "generate": "npm run build:articles && npm run build:projects && nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "build:articles": "node scripts/build-articles-json.mjs",
    "build:projects": "node scripts/build-projects-json.mjs"
  },
  "dependencies": {
    "@nuxt/content": "^3.6.3",
    "@nuxt/image": "^1.10.0",
    "@nuxtjs/i18n": "^10.0.6",
    "@nuxtjs/tailwindcss": "^6.14.0",
    "@tailwindcss/typography": "^0.5.19",
    "better-sqlite3": "^12.2.0",
    "nuxt": "^4.0.1",
    "remixicon": "^4.6.0",
    "sharp": "^0.34.5",
    "vue": "^3.5.18",
    "vue-router": "^4.5.1"
  },
  "devDependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^17.0.0",
    "prettier": "3.6.2"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
